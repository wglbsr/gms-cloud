server:
  port: 8610
spring:
  profiles:
    active: @package.environment@
  main:
    allow-bean-definition-overriding: true
  application:
    name: biz-g1
management:
  endpoints:
    web:
      exposure:
        include: \*