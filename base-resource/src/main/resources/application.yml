server:
  port: 8015 #8181
spring:
  application:
    name: base-resource
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
management:
  endpoints:
    web:
      exposure:
        include: \*
